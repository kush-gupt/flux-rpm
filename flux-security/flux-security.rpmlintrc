# The flux-imp binary is intentionally setuid root.
# It is the Independent Minister of Privilege for the Flux resource manager,
# designed to execute privileged operations on behalf of Flux.
# See: https://flux-framework.readthedocs.io/projects/flux-security/
#
# Note: These filters apply to built RPMs. When running rpmlint on only the
# spec file, "unused-rpmlintrc-filter" warnings are expected and can be ignored.
addFilter("setuid-binary.*/usr/libexec/flux/flux-imp")
addFilter("non-standard-executable-perm.*/usr/libexec/flux/flux-imp")
